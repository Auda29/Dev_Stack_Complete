# Dev_Stack Environment Variables
# Copy this file to .env and fill in your values

# LLM Provider Configuration
# Choose which LLM provider to use: openai, anthropic, or google
LLM_PROVIDER=openai

# OpenAI Configuration (required if LLM_PROVIDER=openai)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Specify model (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# Anthropic Configuration (required if LLM_PROVIDER=anthropic)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Optional: Specify model (default: claude-3-5-sonnet-20240620)
ANTHROPIC_MODEL=claude-3-5-sonnet-20240620

# Google Gemini Configuration (required if LLM_PROVIDER=google)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
# Optional: Specify model (default: gemini-1.5-pro)
GOOGLE_MODEL=gemini-1.5-pro

# ChromaDB Configuration
# Host where ChromaDB is running (use 'chroma' inside Docker, 'localhost' on host)
CHROMA_HOST=chroma
# ChromaDB port (default: 8000)
CHROMA_PORT=8000

# Agent Configuration
LOG_LEVEL=INFO
